//Algoritmo 4

//criando uma lista de 5 elementos
const arr = [1,2,3,4,5]
//usando o filter para pegar apenas os valores maiores que 4, excluindo até mesmo valor 4 desse filter
const output = arr.filter((x)=>  x > 4);
console.log(output);

//nesse código é criado uma lista de 5 elementos, onde é feito um filter para pegar apenas os valores maiores que 4, assim pegando apenas o valor 5 da lista.

